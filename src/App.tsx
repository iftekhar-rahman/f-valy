import AppRouter from './AppRouter';
import Header from './components/common/Header';

function App() {
  const name = '';
  console.log(name);

  return (
    <div>
      <AppRouter>
        <Header />
      </AppRouter>
    </div>
  );
}

export default App;
